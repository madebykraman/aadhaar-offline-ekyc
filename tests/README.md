# Test fixtures

`fixtures/synthetic-ekyc.xml` is deliberately fabricated and safe to open during manual QA. It exercises the modern OfflinePaperlessKyc parser, address formatting, masked contact hashes, copy controls, metadata, and clear-session flow.

Do not add real Aadhaar XML, ZIP files, Share Codes, photographs, or personal data to this repository.
